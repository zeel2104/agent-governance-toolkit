[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-os-kernel"
version = "2.2.0"
description = "Community Edition — A kernel architecture for governing autonomous AI agents with Nexus Trust Exchange"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [
    {name = "Microsoft Corporation", email = "agt@microsoft.com"},
]
maintainers = [
    {name = "Agent Governance Toolkit Team", email = "agt@microsoft.com"},
]
keywords = [
    "ai",
    "agents",
    "llm",
    "kernel",
    "safety",
    "governance",
    "control-plane",
    "multi-agent",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]

# Core dependencies (minimal - just what's needed for kernel)
dependencies = [
    "pydantic>=2.4.0",
]

[project.urls]
Homepage = "https://github.com/microsoft/agent-governance-toolkit"
Documentation = "https://github.com/microsoft/agent-governance-toolkit/tree/master/docs"
Repository = "https://github.com/microsoft/agent-governance-toolkit"
"Bug Tracker" = "https://github.com/microsoft/agent-governance-toolkit/issues"

[project.optional-dependencies]
# Individual components
cmvk = [
    "numpy>=1.20.0",
]

iatp = [
    "fastapi>=0.115.0",
    "uvicorn>=0.27.0",
    "httpx>=0.27.0",
    "cryptography>=46.0.5",
    "pynacl>=1.5.0",
]

amb = [
    "anyio>=3.0.0",
    "aiofiles>=23.0.0",
]

observability = [
    "prometheus-client>=0.17.0",
    "opentelemetry-api>=1.20.0",
    "opentelemetry-sdk>=1.20.0",
]

mcp = [
    "mcp>=1.0.0",
]

nexus = [
    "agent-os-kernel[iatp]",
    "pyyaml>=6.0.0",
    "structlog>=24.1.0",
    "aiohttp>=3.13.3",
]

# Bundles
full = [
    "agent-os-kernel[cmvk,iatp,amb,observability,mcp,nexus]",
]
hypervisor = [
    "agent-runtime>=1.0.0",
]

# Development (includes all optional deps for full testing)
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "mypy>=1.0.0",
    "ruff>=0.1.0",
    "import-linter>=2.0",
    "pyyaml>=6.0.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "numpy>=1.20.0",
    "httpx>=0.27.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.27.0",
    "anyio>=3.0.0",
    "aiohttp>=3.13.3",
    "structlog>=24.1.0",
    "redis>=4.0.0",
    "cryptography>=44.0.0",
    "eval_type_backport>=0.2.0; python_version < '3.10'",
]

[project.scripts]
agent-os = "agent_os.cli:main"

[tool.hatch.build.targets.wheel]
packages = [
    "src/agent_os",
    # Layer 1 – Primitives
    "modules/primitives/agent_primitives",
    "modules/cmvk/src/cmvk",
    "modules/caas/src/caas",
    "modules/emk/emk",
    # Layer 2 – Infrastructure
    "modules/iatp/iatp",
    "modules/amb/amb_core",
    "modules/atr/atr",
    # Layer 3 – Framework
    "modules/control-plane/src/agent_control_plane",
    "modules/observability/src/agent_os_observability",
    "modules/nexus",
    # Layer 4 – Intelligence
    "modules/scak/agent_kernel",
    "modules/mute-agent/mute_agent",
    # Servers
    "modules/mcp-kernel-server/src/mcp_kernel_server",
]

# Remap source paths so each subpackage installs at the wheel root.
# Without this, hatch preserves intermediate dirs (e.g. control-plane/src/).
[tool.hatch.build.targets.wheel.sources]
"src" = ""
"modules/primitives" = ""
"modules/cmvk/src" = ""
"modules/caas/src" = ""
"modules/emk" = ""
"modules/iatp" = ""
"modules/amb" = ""
"modules/atr" = ""
"modules/control-plane/src" = ""
"modules/observability/src" = ""
"modules/nexus" = "nexus"
"modules/scak" = ""
"modules/mute-agent" = ""
"modules/mcp-kernel-server/src" = ""

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/modules",
    "/services",
    "/docs",
    "/README.md",
    "/LICENSE",
]

# ============================================================================
# Ruff - Fast Python Linter & Formatter
# ============================================================================
[tool.ruff]
target-version = "py39"
line-length = 100
src = ["src", "modules"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
]
ignore = [
    "E501",   # line too long (handled by formatter)
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["E402", "F401"]
"src/agent_os/integrations/*.py" = ["E402"]

[tool.ruff.lint.isort]
known-first-party = ["agent_os"]

# ============================================================================
# Pytest
# ============================================================================
[tool.pytest.ini_options]
testpaths = ["tests", "modules/*/tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"
markers = [
    "benchmark: performance benchmark tests",
]

# ============================================================================
# MyPy
# ============================================================================
[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false

# ============================================================================
# Coverage
# ============================================================================
[tool.coverage.run]
source = ["src/agent_os", "modules"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
